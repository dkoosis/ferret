# ferret — north star (mini)

Ferret mines Claude Code logs of conversations and tool use to find patterns of success and friction. It writes those findings for the agent itself to act on, so friction gets fixed once instead of re-paid every session — sessions that compound instead of reset.
