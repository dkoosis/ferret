.DS_Store
/ferret

# Sandbox: cache + transient artifacts (prebuilt binaries are tracked in .sandbox/bin/)
.sandbox/cache/
.sandbox/setup-report.json
.sandbox/bin/**/*.tmp
.sandbox/bin/**/*.upx
.sandbox/bin/**/*.000
.claude/tmp/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
